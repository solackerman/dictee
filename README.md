# French Dictee Practice

## Try it out locally

1. Start a simple web server from the repo root:
   ```bash
   python -m http.server 8000 --directory /workspace/dictee
   ```
2. Open the app in your browser:
   ```
   http://localhost:8000/index.html
   ```

That’s it! Paste your French words (one per line), hit **Practice**, and follow the prompts.
